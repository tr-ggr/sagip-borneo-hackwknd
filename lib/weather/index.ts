/**
 * Open-Meteo – barrel export
 *
 * Import everything from a single entry point:
 *
 * @example
 * import {
 *   getWeatherForecast,
 *   getWeatherForPlace,
 *   TemperatureUnit,
 *   WeatherCode,
 * } from "@/lib/weather";
 */

export * from "./types";
export * from "./actions";
